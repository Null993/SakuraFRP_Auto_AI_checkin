services:
  sakurafrp-checkin:
    build: .
    image: sakurafrp-auto-ai-checkin:latest
    container_name: sakurafrp-checkin
    restart: unless-stopped
    environment:
      TZ: Asia/Shanghai
      APP_DATA_DIR: /data
    volumes:
      - ./data:/data
